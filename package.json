{
  "name": "grantpulse-dapp",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:contract": "cd contract && cargo build --workspace --target wasm32-unknown-unknown --release",
    "test": "npm --prefix frontend run build && cd contract && cargo test --workspace",
    "test:contract": "cd contract && cargo test --workspace",
    "test:frontend": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "deploy:testnet": "pwsh ./scripts/deploy.ps1"
  }
}
