[package]
name = "proposal_contract"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20.5.0"

[dev-dependencies]
soroban-sdk = { version = "20.5.0", features = ["testutils"] }
reputation_token = { path = "../reputation_token", features = ["testutils"] }
treasury_contract = { path = "../treasury_contract", features = ["testutils"] }
